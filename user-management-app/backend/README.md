# User Management App - Backend

This is the backend API for the User Management App, built with Node.js and Express.js.

## Features
- RESTful API for user management
- User model with fields: name, email, date of birth, mobile number
- CRUD operations for users
- Error handling middleware (Express v5 automatically catches errors)

### in express v-5 it automatically catch the error and send to the middleware
