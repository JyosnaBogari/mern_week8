//create context object
// create is a function  in react
//upper camelcase use for naming
//they are like pipeline ,when we pass a state it passes to the component ,its like a channel
//
import { createContext } from "react";
export const CounterContext=createContext();